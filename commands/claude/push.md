---
description: Show pending commits and push after explicit approval.
---

Run the push stage of the `commit` skill: show `git status`, the commits
ahead of `origin/<branch>`, and the target branch, then push only after the
user approves. $ARGUMENTS
