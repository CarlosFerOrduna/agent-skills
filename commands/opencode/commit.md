---
description: Propose a Conventional Commit message and wait for approval before committing.
agent: build
---

Run the `commit` skill end-to-end: gather the git state, propose the message
and its file summary, and stop for approval before staging or committing.
Validate the message with `commit-conventions`. $ARGUMENTS
